{
  "private": true,
  "name": "auto-version-action",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "lint": "eslint .",
    "prepare": "ncc build src/index.js -m -o dist --source-map --license licenses.txt",
    "format": "prettier --check src",
    "test": "jest",
    "all": "npm run lint && npm run format && npm run test && npm run prepare"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/npetzall/auto-version-action.git"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/npetzall/auto-version-action/issues"
  },
  "homepage": "https://github.com/npetzall/auto-version-action#readme",
  "dependencies": {
    "@actions/core": "^1.2.5",
    "@actions/github": "^5.0.3"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.31.1",
    "eslint": "^8.0.0",
    "eslint-config-prettier": "^8.5.0",
    "jest": "^27.2.5",
    "prettier": "^2.7.1",
    "rewire": "^6.0.0"
  },
  "jest": {
    "setupFiles": [
      "<rootDir>/.jest/envVars.js"
    ]
  }
}
